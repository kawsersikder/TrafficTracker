# Experiment suite dependencies (Python >= 3.10)
# NOTE: install PyTorch separately with the CUDA index (see TRAINING.md):
#   pip install torch --index-url https://download.pytorch.org/whl/cu126

pandas>=2.2
numpy>=1.26
pyarrow>=15
scikit-learn>=1.4
xgboost>=2.0
statsmodels>=0.14
matplotlib>=3.8
tqdm>=4.66
tabulate>=0.9

# Jakarta / Bandung / Semarang GeoPackage characterization
geopandas>=1.0

# Dhaka vision track (YOLOv8)
ultralytics>=8.2
